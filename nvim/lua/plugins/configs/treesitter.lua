local options = {
  ensure_installed = { "lua", "vimdoc", "python", "typescript", "javascript", "css", "scss", "json"},
  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
