local options = {
  ensure_installed = { "lua", "javascript", "typescript", "tsx" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
